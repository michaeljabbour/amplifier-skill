interface:
  display_name: "Amplifier Skill"
  short_description: "Delegate complex work via Amplifier safely"
  default_prompt: "Use $amplifier-skill to delegate this task to Amplifier, then summarize findings and next actions."
